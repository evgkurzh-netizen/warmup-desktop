{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "welcome",
  "description": "First-launch setup window: can call setup commands.",
  "windows": ["welcome"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "shell:default"
  ]
}
